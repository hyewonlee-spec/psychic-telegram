# Memory Calendar — Modal Image Position Lock Patch

Replace this file only:

- src/styles.css

What this fixes:
- Locks the modal photo inside the portrait preview frame.
- Removes the blank band/gap at the top of the photo container.
- Makes the image fill the container from the centre.
- Prevents the image from sitting low inside the frame.
- Slightly increases the portrait viewer size again on mobile.
- No App.tsx, Supabase, database, storage, auth, or PWA changes.
